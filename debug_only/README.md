# Historical research scripts

These files are preserved for provenance. They contain older experimental
environment copies and simulation rules and are not the supported entry points.
Some calls no longer match the core API; see [the findings log](../BUGS.md).

Use `python train.py` for training, `python train_hardware_usage.py` for optional
monitoring, and `python -m unittest discover -s test -v` for the current tests.
